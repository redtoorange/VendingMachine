Compile the Application:
    1.  Open a command prompt/terminal and navigate to the directory where 
        MainGUI.java is stored.
    2.  Run the command:  javac MainGUI.java

Run the Application:
    1.  Open a command prompt/terminal and navigate to the directory where 
        MainGUI.class is stored.
    2.  Run the command: java MainGUI